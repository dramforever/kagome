{
    "private": true,
    "name": "kagome-demo",
    "version": "0.0.1",
    "dependencies": {
        "kagome": "*",
        "tslib": "^1.11.2"
    },
    "devDependencies": {
        "typescript": "^3.8.3"
    },
    "scripts": {
        "prepack": "tsc --build",
        "watch": "tsc --build --watch --preserveWatchOutput"
    }
}
